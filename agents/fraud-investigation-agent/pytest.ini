[pytest]
# harness-core/python: 하네스 공유 계약(harness_core). 이 디렉터리 밖에 있다.
# 사본을 만들지 않기로 한 결정의 대가다 — docs/decisions/agent-harness-consolidation.md 2-d.
# 이 에이전트는 Dockerfile 이 없어 소스에서 바로 돌기 때문에, 다른 두 Python
# 에이전트와 달리 빌드 컨텍스트로는 해결되지 않는다 (5단계에서 드러난 것).
pythonpath = src ../harness-core/python
testpaths = tests
