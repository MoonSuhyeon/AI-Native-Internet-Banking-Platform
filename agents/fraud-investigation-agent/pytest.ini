[pytest]
# harness_core 는 pythonpath 로 얹지 않는다. venv 에 설치해서 쓴다
# (requirements-dev.txt 의 -e ../harness-core/python).
# 세 에이전트가 세 가지 방법으로 같은 패키지를 보던 것을 하나로 모았다
# — docs/decisions/agent-harness-consolidation.md 다음 순서 6.
pythonpath = src
testpaths = tests
