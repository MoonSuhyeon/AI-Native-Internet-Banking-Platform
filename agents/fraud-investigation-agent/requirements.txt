langgraph>=0.2
pydantic>=2.7
rich>=13.0
httpx>=0.27
fastapi>=0.110
uvicorn>=0.29
# 조사 성능 지표(/metrics). consultation 과 같은 라이브러리를 쓴다.
prometheus-client>=0.20
# Harness audit storage. Imported only when FRAUD_AUDIT_DSN is set
# (harness_core.audit_psycopg imports it lazily); without it the audit log is a
# no-op and the demo path still runs.
#
# NOTE: keep this file ASCII-only. pip decodes requirements files with the
# locale encoding, so Korean comments break `pip install` on cp949 Windows.
psycopg[binary]>=3.2

# Phoenix tracing. Only imported when PHOENIX_ENABLED is true (agent/tracing.py
# imports lazily); without it the investigation loop still runs and tracing is
# a no-op.
arize-phoenix-otel>=0.16.0
