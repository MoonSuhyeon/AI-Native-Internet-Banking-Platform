langgraph>=0.2
pydantic>=2.7
rich>=13.0
httpx>=0.27
fastapi>=0.110
uvicorn>=0.29
pytest>=8.0
# 하네스 감사 기록 저장용. FRAUD_AUDIT_DSN 이 설정된 경우에만 실제로 import 된다
# (harness_core.audit_psycopg 가 지연 import). 미설정 시 감사는 NoOp 이라
# 이 패키지가 없어도 데모 경로는 그대로 돈다.
psycopg[binary]>=3.2
