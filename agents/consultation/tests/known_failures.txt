# consultation 알려진 실패 목록 — 지금은 비어 있다
#
# 이 목록의 테스트는 strict xfail 로 표시된다(tests/conftest.py 참조). 뜻은 둘이다.
#   - 실패해도 CI 를 빨갛게 만들지 않는다
#   - **통과하기 시작하면 CI 가 빨개진다** (strict). 고쳤으면 이 목록에서 지워야 한다.
#     그래서 이 목록은 조용히 영구 부채가 되지 못한다.
#
# 왜 이런 목록이 있었나:
#   consultation 테스트를 돌리는 CI 가 없어서 24개 파일이 두 달 넘게 썩었다.
#   에러 1,357건을 걷어내니 1,508건이 통과하고 91건이 남았다. 이 91건을 다 고칠
#   때까지 CI 를 미루면 그동안 1,508건도 지켜지지 않는다. 그래서 CI 를 먼저 걸고
#   목록을 줄여왔다. 91 → 79 → 54 → 32 → 12 → 0.
#
# 비었다고 파일을 지우지는 않았다. 다시 필요해질 때 장치를 새로 만드는 것보다
# 규칙과 함께 남겨두는 편이 낫다.
#
# 다시 채울 때의 규칙:
#   - 먼저 고쳐본다. 여기 적는 건 마지막 수단이다.
#   - 적는다면 왜 지금 못 고치는지를 주석으로 함께 남긴다.
#   - 이 파일은 늘어나면 안 된다.
