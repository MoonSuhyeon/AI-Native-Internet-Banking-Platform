# 보완 필요 항목

> 코드 주석·문서에 "아직 없다 / 미구현 / 후속 / 임시" 로 적힌 것을 한자리에 모았다.
> 흩어져 있으면 각자 자기 파일에서만 보이고, 무엇이 얼마나 남았는지 아무도 모른다.
>
> **여기 없는 것은 "안 하기로 한 것" 이 아니라 "아직 적히지 않은 것" 이다.**
> 새로 미룬 것이 생기면 코드에만 적지 말고 이 표에도 한 줄 남긴다.

수집 기준 — 소스의 `TODO`, 그리고 주석·문서의 `미구현 / 아직 없다 / 후속 / 임시 / 미해소`.
2026-08-09 기준.

---

## 1. 지금 데모·운영에 영향이 있는 것

| 항목 | 무엇이 안 되는가 | 어디 |
|---|---|---|
| **지연 알림 외부 발송** | 앱 알림함에만 쌓인다. SMS·이메일 게이트웨이가 없어 고객이 접속하지 않으면 지연 사실을 모른다 | `NotificationService` |
| **본심사 API** | 대출 본심사가 없어 테스트가 repository 로 `APPROVED` 를 강제 전이시킨다 | loan-service 통합테스트 |
| **예약이체 타행/BOK** | 자행만 구현. 타행 예약이체는 `UnsupportedOperationException` | `PaymentOrchestratorImpl` |
| **일일 한도 세분화** | `perTx/daily/monthly` 구분 없이 `dailyWithdrawLimit` 하나로 비교 (D-REQ-4 미해소) | `step2b_executeValidation` |
| **잔액·한도 전용 API 부재** | deposit 에 `/balances`·`/limits` 가 없어 계좌조회 응답으로 대체 (D-REQ-3 미해소) | 〃 |

## 2. 통제·감사에서 비어 있는 것

| 항목 | 왜 문제인가 | 어디 |
|---|---|---|
| **사망 플래그 미연동** | `deceasedFlag` 가 `false` 고정. Party 에 사망 컬럼이 없다 — 조사 에이전트의 fail-closed 판단 근거 하나가 늘 꺼져 있다 | `InternalHolderController` |
| **비밀번호 변경 이력 미연동** | `passwordChangedRecently` 가 `false` 고정. 계정탈취 신호 하나가 죽어 있다 | `InternalAuthEventsController` |
| **관리자 화면 직원 ID 임시 입력** | JWT·게이트웨이 역할 연동 전이라 화면에서 직접 입력받는다 — 행위자를 위조할 수 있다 | `admin/consultation/customer` |
| **상담 서비스 인증 미들웨어** | JWT 기반 인증이 없다 | `agents/consultation/app/main.py` |
| **P-029 self-listening** | 자기가 보낸 메시지를 자기가 받는 것을 sender 은행코드로만 막는다 | `KftcNetworkResponseConsumer` |
| **역분개 미구현** | `reversal_yn` 스키마만 있고 코드가 없다 | 결제 원장 |

## 3. 계측이 비어 있는 것

| 항목 | 현재 | 어디 |
|---|---|---|
| **검색 품질 정기 측정** | 평가셋·계산은 있으나 실제 백엔드로 주기 실행하는 자리가 없다 | `RetrievalEvalRunner` |
| **doc-agent 지표 6종** | 적체 게이지·결정 소요시간·위조점수 분포 등이 설계만 있고 코드 없음 | `docs/monitoring/doc-agent-metrics.md` §6 |
| **Resilience4j 지표** | 서킷브레이커가 코드엔 있으나 스크레이프에서 확인되지 않았다 — 사이드카를 띄운 상태로 재확인 필요 | doc-agent |
| **로그·지표·트레이스 상관분석** | 지표에서 이상을 보고 그 시점 트레이스로 갈 수 없다 (Loki·Tempo 미도입) | — |
| **카나리 배포** | 없다. 기능 플래그로 전부 켜거나 끄는 것만 가능 | — |

## 4. 임시로 둔 값 — 운영 전에 정해야 하는 것

| 항목 | 현재 값 | 근거 |
|---|---|---|
| **FDS 절대 금액 임계** | 1천만원 | 프로파일(평소 대비 편차)이 들어오면 대체. 실무는 절대값을 잘 안 쓴다 |
| **1일 누적 보고 기준** | 2천만원 | **데모 값이다.** 실제 규정 금액을 현행 자료로 확인해 넣어야 한다 |
| **관련성 임계** | 0.3 | 백엔드마다 척도가 달라 분포를 보고 맞춰야 한다 |
| **지연 시간** | 30분 | 짧으면 고객이 못 알아채고 길면 정상 거래가 불편하다 |
| **이체 금액 구간** | 10만 / 500만 / 5천만 | 정책 결정 필요 |
| **연체 가산금리** | 300bps 고정 | 상품별 차등은 후속 |
| **거래번호 채번** | `TXN-` + piId 파생 | 정식 채번 메서드 없음 |

## 5. 설계만 있고 코드가 없는 것

| 항목 | 상태 |
|---|---|
| **전사 공통 모델(`common_*`)** | 통합 모델 설계만 있고 코드 이관 없음 |
| **마스터 코드 연동** | `code_master` 가 검증에 쓰이지 않는다 (`@ValidCode` 0곳). 시드만으론 효과 없음 |
| **`party_type='PERSON'` 꼼수** | 직원 구분을 party_type 으로 하고 있다. `party_role` 기반으로 정상화 필요 (별도 티켓) |
| **만기 자동 전이·알림** | `ACTIVE→MATURED` 자동 전이와 만기 알림이 별도 배치로 후속 |
| **자동이체·중도상환·부분상환** | 후속 |

## 6. 문서가 코드와 어긋난 것

| 문서 | 문제 |
|---|---|
| `api-spec.md`, `deposit-service-api-spec.md`, `payment-service-api-spec.md` | **`deposit-service`·`payment-service` 를 별도 서비스로 설명한다.** 둘은 `core-banking` 으로 병합됐다([결정 문서](decisions/core-banking-merge.md)) |
| `docs/plan/*` 32건 | 27건이 완료 표시를 달고 있으나 진행 중인 것과 섞여 있다 |

---

## 우선순위

이 목록을 다 하자는 게 아니다. **막고 있는 것부터** 본다.

1. **사망 플래그·비밀번호 변경 이력 연동** — 조사 에이전트의 판단 근거가 두 개 꺼져 있다.
   에이전트를 아무리 다듬어도 입력이 없으면 소용없다.
2. **API 스펙의 병합 반영** — 지금 스펙을 보고 붙이면 없는 서비스를 부르게 된다.
3. **검색 품질 정기 측정** — 평가셋이 준비됐으므로 실행 자리만 만들면 된다.
4. **관리자 화면 직원 ID** — 행위자 위조가 가능한 상태로 감사 로그가 쌓인다.

나머지는 "후속" 으로 적힌 대로 두어도 지금 무언가를 막지 않는다.
