<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bank.payment.domain.mapper.PaymentInstructionMapper">

    <insert id="insert" parameterType="com.bank.payment.domain.PaymentInstruction">
        INSERT INTO payment_instruction (
            payment_instruction_id,
            idempotency_key,
            sender_user_id,
            sender_account_id,
            auth_token_id,
            original_payment_id,
            transaction_no,
            sender_account_no_snap,
            sender_account_alias_snap,
            receiver_bank_code,
            receiver_account_no,
            receiver_holder_name_snap,
            holder_inquiry_at,
            is_intra_bank,
            routing_network_type,
            transfer_amount,
            fee_amount,
            receiver_passbook_sender_display,
            receiver_memo,
            sender_memo,
            status,
            failure_category,
            channel,
            requested_at,
            completed_at,
            business_date,
            next_retry_at,
            next_timeout_at,
            version,
            trigger_source,
            is_scheduled,
            scheduled_execution_at,
            created_by,
            updated_by
        ) VALUES (
            #{paymentInstructionId},
            #{idempotencyKey},
            #{senderUserId},
            #{senderAccountId},
            #{authTokenId},
            #{originalPaymentId},
            #{transactionNo},
            #{senderAccountNoSnap},
            #{senderAccountAliasSnap},
            #{receiverBankCode},
            #{receiverAccountNo},
            #{receiverHolderNameSnap},
            #{holderInquiryAt},
            #{isIntraBank},
            #{routingNetworkType},
            #{transferAmount},
            #{feeAmount},
            #{receiverPassbookSenderDisplay},
            #{receiverMemo},
            #{senderMemo},
            #{status},
            #{failureCategory},
            #{channel},
            #{requestedAt},
            #{completedAt},
            #{businessDate},
            #{nextRetryAt},
            #{nextTimeoutAt},
            #{version},
            #{triggerSource},
            #{isScheduled},
            #{scheduledExecutionAt},
            #{createdBy},
            #{updatedBy}
        )
    </insert>

    <select id="selectById" resultType="com.bank.payment.domain.PaymentInstruction">
        SELECT *
        FROM payment_instruction
        WHERE payment_instruction_id = #{paymentInstructionId}
    </select>

    <update id="updateStatus">
        UPDATE payment_instruction
        SET status            = #{status},
            completed_at      = #{completedAt},
            failure_category  = #{failureCategory},
            next_retry_at     = NULL,
            next_timeout_at   = NULL,
            version           = version + 1,
            updated_at  = now()
        WHERE payment_instruction_id = #{paymentInstructionId}
          AND version = #{version}
    </update>

    <!-- version/status 건드리지 않음. CLEARING 전이 직후 F6 폴링워커용 타임아웃 세팅 전용. -->
    <update id="updateNextTimeoutAt">
        UPDATE payment_instruction
        SET next_timeout_at  = #{nextTimeoutAt},
            updated_at = now()
        WHERE payment_instruction_id = #{piId}
    </update>

    <!-- F6 폴링워커: 타임아웃 대상 조회. idx_pi_next_timeout_at 부분인덱스 활용. -->
    <select id="selectTimedOut" resultType="com.bank.payment.domain.PaymentInstruction">
        SELECT *
        FROM payment_instruction
        WHERE next_timeout_at IS NOT NULL
          AND next_timeout_at &lt; now()
          AND status = 'CLEARING'
        ORDER BY next_timeout_at ASC
        LIMIT 50
    </select>

    <!-- version 건드리지 않음. step2 A-2 수신조회 직후 단건 박제용. authorize 낙관락에 영향 없음. -->
    <update id="updateReceiverHolderSnap">
        UPDATE payment_instruction
        SET receiver_holder_name_snap = #{receiverHolderNameSnap},
            holder_inquiry_at         = #{holderInquiryAt},
            updated_at          = now()
        WHERE payment_instruction_id = #{paymentInstructionId}
    </update>

    <select id="countIncomplete" resultType="int">
        SELECT COUNT(*)
        FROM payment_instruction
        WHERE status NOT IN ('COMPLETED', 'FAILED', 'CANCELED')
    </select>

    <!--
      고객의 당일 이체 누적액 (인터넷뱅킹 이체한도 판정용).

      계좌가 아니라 **고객(sender_user_id)** 으로 묶는다. 한도가 고객당이므로 계좌로
      집계하면 계좌를 두 개 가진 사람이 한도를 두 배로 쓴다.

      실패·취소는 뺀다. 나가지 않은 돈까지 한도를 깎으면, 시스템 오류로 실패한 건 때문에
      고객이 그날 이체를 못 하게 된다.

      진행 중(PENDING·AUTHORIZED·PROCESSING·CLEARING)은 **포함**한다. 아직 확정되지
      않았어도 이미 출금은 나갔거나 나갈 예정이라, 빼면 동시에 여러 건을 밀어 넣어
      한도를 넘길 수 있다.

      수수료는 세지 않는다. 한도는 "얼마를 보냈는가" 에 대한 것이고, 수수료까지 세면
      고객이 화면에서 계산한 금액과 어긋난다.

      **지금 처리 중인 건은 뺀다.** 결제지시는 검증보다 먼저 INSERT 되므로, 빼지 않으면
      이번 금액이 누적에도 들어가 두 번 세어진다 — 한도의 절반만 써도 초과로 막힌다.
    -->
    <select id="sumDailyTransferAmount" resultType="long">
        SELECT COALESCE(SUM(transfer_amount), 0)
        FROM payment_instruction
        WHERE sender_user_id = #{senderUserId}
          AND business_date  = #{businessDate}
          AND status NOT IN ('FAILED', 'CANCELED')
          AND payment_instruction_id &lt;&gt; #{excludePiId}
    </select>

    <!-- 예약이체 워커: 실행 시각이 도래한 SCHEDULED PI 폴링. selectTimedOut 대칭. -->
    <select id="selectDueScheduled" resultType="com.bank.payment.domain.PaymentInstruction">
        SELECT
            payment_instruction_id,
            idempotency_key,
            sender_user_id,
            sender_account_id,
            auth_token_id,
            original_payment_id,
            transaction_no,
            sender_account_no_snap,
            sender_account_alias_snap,
            receiver_bank_code,
            receiver_account_no,
            receiver_holder_name_snap,
            holder_inquiry_at,
            is_intra_bank,
            routing_network_type,
            transfer_amount,
            fee_amount,
            receiver_passbook_sender_display,
            receiver_memo,
            sender_memo,
            status,
            failure_category,
            channel,
            requested_at,
            completed_at,
            business_date,
            next_retry_at,
            next_timeout_at,
            version,
            trigger_source,
            is_scheduled,
            scheduled_execution_at,
            created_at,
            created_by,
            updated_at,
            updated_by
        FROM payment_instruction
        WHERE status = 'SCHEDULED'
          AND scheduled_execution_at &lt; now()
        ORDER BY scheduled_execution_at ASC
        LIMIT 50
    </select>

    <!-- 예약이체 워커 선점(claim) 전용 UPDATE. status 가드(SCHEDULED) 포함. version+1. -->
    <!-- next_timeout_at/next_retry_at 건드리지 않음(updateStatus 와 달리 NULL 세팅 없음). -->
    <!-- scheduled_execution_at/is_scheduled 는 감사 목적으로 그대로 보존. -->
    <update id="claimScheduledForExecution">
        UPDATE payment_instruction
           SET status           = 'PROCESSING',
               version          = version + 1,
               updated_at = now()
         WHERE payment_instruction_id = #{paymentInstructionId}
           AND status               = 'SCHEDULED'
           AND version              = #{version}
    </update>

    <!-- 사용자 예약취소 전용 UPDATE. claim 과 대칭: status/version 가드 동일, CANCELED 전이. -->
    <!-- next_timeout_at/next_retry_at/scheduled_execution_at 건드리지 않음(감사 보존). -->
    <update id="cancelScheduledForUser">
        UPDATE payment_instruction
           SET status           = 'CANCELED',
               version          = version + 1,
               updated_at = now()
         WHERE payment_instruction_id = #{paymentInstructionId}
           AND status               = 'SCHEDULED'
           AND version              = #{version}
    </update>

    <!-- 수신계좌 기준 입금(COMPLETED) 내역 조회. 다온 화면 전용. SELECT * 금지. -->
    <select id="selectByReceiverAccountNo" resultType="com.bank.payment.domain.PaymentInstruction">
        SELECT
            payment_instruction_id,
            transaction_no,
            transfer_amount,
            status,
            requested_at,
            completed_at,
            sender_account_no_snap,
            receiver_passbook_sender_display,
            receiver_memo
        FROM payment_instruction
        WHERE receiver_account_no = #{receiverAccountNo}
          AND status = 'COMPLETED'
        ORDER BY completed_at DESC
    </select>

    <!-- AUTHORIZED→SCHEDULED 전이. is_scheduled=true + scheduled_execution_at 세팅. 낙관락 포함. -->
    <update id="updateScheduled">
        UPDATE payment_instruction
           SET status                 = 'SCHEDULED',
               is_scheduled          = true,
               scheduled_execution_at = #{scheduledExecutionAt},
               updated_at      = now(),
               version               = version + 1
         WHERE payment_instruction_id = #{paymentInstructionId}
           AND status                 = 'AUTHORIZED'
           AND version                = #{version}
    </update>

</mapper>
